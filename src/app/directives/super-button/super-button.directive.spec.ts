import { SuperButtonDirective } from "./super-button.directive";

describe('SuperButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperButtonDirective();
    expect(directive).toBeTruthy();
  });
});
